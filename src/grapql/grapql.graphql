type Grapql {
  # Example field (placeholder)
  exampleField: Int
}

input CreateGrapqlInput {
  # Example field (placeholder)
  exampleField: Int
}

input UpdateGrapqlInput {
  id: Int!
}

type Query {
  grapql: [Grapql]!
  grapql(id: Int!): Grapql
}

type Mutation {
  createGrapql(createGrapqlInput: CreateGrapqlInput!): Grapql!
  updateGrapql(updateGrapqlInput: UpdateGrapqlInput!): Grapql!
  removeGrapql(id: Int!): Grapql
}
